import { main as lab10Main } from "./lab10";
import { main as lab11Main } from "./lab11";

document.addEventListener("DOMContentLoaded", () => {
  console.log("Index page loaded");
  // Пример вызова main для Lab10 или Lab11
  // Если нужно автоматическое выполнение какой-то лабораторной:
  // lab10Main(); или lab11Main();
});
